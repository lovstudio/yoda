---
"yoda": patch
---

新增 Antigravity CLI（Google Gemini 驱动的终端编码 agent，命令 `agy`）作为可用 runtime：注册元数据（安装命令、检测、`-i` 初始 prompt、`--continue` 续接、`--dangerously-skip-permissions` 自动批准）、账号 profile（Google API 环境变量）、logo 与本地配置路径。
